# DamageAdvisor 进度与状态

## v0.9.0（已部署）— 卡牌词条（keywords）支持

对猎人卡池 91 张牌做了词条盘点：`Exhaust ×11`、`Sly ×7`、`Innate ×3`、`Retain ×2`。

| 词条 | 处理 |
|---|---|
| **Sly（奇巧）** | **之前只硬编码了战术大师/本能反应，其余奇巧牌被弃时效果全部丢失**。现在按游戏语义实现：`Sly` 牌被丢弃会**自动打出**（`AutoPlayType.SlyDiscard`，不花能量），伤害/格挡/抽牌/能量/中毒/敏捷/小刀都按自动打出结算；弃牌选择也改为**优先弃奇巧牌**并挑价值最高的那张 |
| Unplayable | 用 `card.Keywords.Contains(CardKeyword.Unplayable)` 判定，标记为不可打出、不参与推荐（`IsPlayable` 是 protected，用不了） |
| Exhaust / Retain / Innate / Ethereal | 本回合不影响结果（消耗不影响"本回合打一次"，保留/固有/虚无影响后续回合），已在 README 说明 |
| 词条显示 | 面板手牌行显示 `[奇巧/消耗/保留/固有/虚无]`；判定源为 `card.IsSlyThisTurn`（含"本回合获得奇巧"的情况） |

### 奇巧牌的完整清单（会自动打出）

`战术大师`（+能量）、`本能反应`（抽牌）、`翻越撑击`（7 伤全体）、`连续反弹`（3 伤×4）、`触不可及`（6 格挡）、`磨蚀`（敏捷+荆棘）、`鬼祟`（能力）

## v0.8.1（已部署）— 读取敌人身上的 debuff

**结论**：敌人身上的中毒/易伤/虚弱/力量可以直接从 `Creature.Powers` 读（`GetPower<T>()?.Amount`），队友施加的也算数——**不需要预测队友行为**。

已改动：

- **模拟的初始中毒 = 敌人当前中毒层数**（之前从 0 起算，导致队友/上回合留下的毒没进"会不会被毒死"的判断）。
- 面板敌人行显示 debuff：`1.化石追踪者 40/40(来袭12) [中毒5 易伤2]`。
- 日志敌人行追加 `buff[中毒=5 易伤=2 虚弱=1 力量=-2]` 与 `按我算/按全队算`。
- 注意：**虚弱/力量不初始化进模拟**——因为 `GetSingleDamage` 已经把它们算进 `BaseIncoming` 了，再初始化会重复计算；模拟里的 `Weak/StrengthLoss` 只代表"本回合新施加的部分"。

### 仍需注意

- 卡面伤害取的是游戏预览值（含易伤），但预览对应"当前/上次悬停的目标"，多怪且易伤不对称时会有偏差。

## v0.8.0（已部署）+ 联机结论修正

- 新增 **F6 优先级模式**：保命优先（默认）/ 输出优先。
- 来袭伤害改为按"自己"作为目标计算。
- **联机结论（由使用者确认）**：《杀戮尖塔 2》联机下敌人攻击对**全队造成相同伤害**，因此"按我算"即为准确值——原先"目标归属不确定、结果偏保守"的判断是错误的，已修正文档与注释。
- 联机下真正仍未覆盖的只有：队友的输出/中毒/易伤，以及"队友先把怪打死"带来的乐观偏差。

## v0.7.2（覆盖率审计后的修复，已部署）

用 `silent-coverage-audit` 的思路把 91 张猎人牌与代码对照，发现并修掉 4 个**真实 bug**：

1. **全体中毒牌完全没生效（严重）**：`迷雾（Haze，全体 4 毒 + 1 虚弱）` 与 `毒性爆发（Outbreak，全体 9 毒）` 这类 `AllEnemies` 牌在搜索里走的是"无目标"分支（targetIndex=0），而 debuff 代码只按编号取单体目标 → **毒与虚弱一个都没加上**。现在改为：`HitsAll` 的牌对全部存活敌人施加 debuff。
2. **毒性爆发的"立即触发中毒"未实现**：现在给完毒后立刻结算一次中毒伤害（中毒伤害无视格挡，另开了 `ApplyDamageIgnoringBlock`）。
3. **弹跳药瓶（BouncingFlask）只算了 1 次**：描述是"随机给予 3 层中毒 ×3 次"，总量应为 9；现在按 `Repeat` 乘算（仅对无毒伤但有 `Repeat` 的牌）。
4. **铭记死亡（MementoMori）漏算弃牌加成**：`本回合每弃一张牌 +4 伤害` 现在按 `SearchState.DiscardedThisTurn` 加算。

同时这次审计也确认了一批"看起来特殊、实际已正确处理"的牌：匕首雨/回响斩击/翻越撑击（全体攻击）、尖啸（全体力量削减）、精密瞄准/狩猎（单体高伤）、连续反弹（多段随机，已标注随机目标）。

### 审计方法（可复用）

用 `docs/silent-pool.txt`（卡池转储）筛出"描述里含特殊机制但代码未点名"的牌，再逐张对照描述与 `vars`：

```powershell
# 筛出可疑牌（含 特殊关键字 且 SilentLogic 未点名）
$rows | Where-Object { # DamageAdvisor 进度与状态

## v0.7.1（已修，待部署）

静态自查发现的 4 个问题，已修复：

1. **敌人格挡未计入**（P1）：`SimEnemy` 增加 `Block`，`ApplyDamage` 先扣格挡再扣血——之前遇到会加格挡的怪会高估伤害、误报"可斩杀"。
2. **弃牌触发的抽牌绕过抽牌副作用**（P1）：`本能反应` 被弃时的抽牌改走 `HandleDraws`，腐蚀波/速行者的抽牌副作用不再漏算，且尊循子弹时间的"本回合不能抽牌"。
3. **搜索去重键过粗**（P2）：去重条件补上 `Block/Poison/Shivs/Vulnerable/Weak`，避免把"同名但状态不同"的牌误判为重复。
4. **cfg 运行中不重载**（P3）：面板每 5 秒检查一次 `DamageAdvisor.cfg` 的修改时间，外部改配置即时生效（日志会打印"配置已重新加载"）。

同时修掉 3 个可空性警告（`TryGetValue` 的 out 参数），现在构建是 0 error / 0 warning。

### 仍未解决（已知）

- 多怪时伤害取数依赖游戏维护的单一 `PreviewValue`（对应当前/上次悬停目标），敌人易伤状态不对称时会有偏差。
- 面板拖动的坐标换算未实机验证。
- 联机不按队友分摊来袭；能力牌的跨回合价值不建模。

## v0.7（已编译，待部署）

### A. 允许掉血上限（面板交互）
- **F7** 循环切换 `0 → 1 → 2 → 3 → 5 → 0`（0 = 原来的严格保命）
- 存到 `mods\DamageAdvisor\DamageAdvisor.cfg`（`hpLossBudget=` / `collapsed=`），启动时读取
- 面板标题实时显示"允许掉血 ≤N"

### B. 跨回合视野（优先击杀）
- 评分顺序改为：**不死 → 掉血 ≤ 预算（超预算则少掉血优先）→ 优先击杀 → 最大伤害**
- 击杀的跨回合价值：打死一只怪 = 省掉它下一轮的攻击（同时本回合它的来袭也归零）

### C. 面板交互
- **鼠标拖动**面板任意位置可移动（轮询实现，不依赖 Godot 输入回调）
- **F10** 折叠/展开（折叠后只留"推荐"和"斩杀"两行）
- 按键总览：`F7 掉血上限` / `F8 隐藏` / `F9 注入测试牌` / `F10 折叠` / 拖动移动

## v0.6（已编译，待部署）

- X 费牌：串刺（8 伤 × 能量）、萎靡（X 力量削减 + X 虚弱）
- 子弹时间：手牌全部免费 + 本回合禁止抽牌
- 钢铁风暴：弃整手换等量小刀
- 计算下注：弃整手抽等量
- 精准：本回合小刀 +4
- 余像：每打一张牌 +1 格挡
- 暴露：施加 2 层易伤
- 修复：精确切击等计算型伤害读 CalculatedDamage；华丽收场真实检查抽牌堆；蜃景不再误标未建模
- F9 注入：手牌满 10 张时先弃最后一张


最后更新：2026-09-15 15:12

## 当前版本：v0.5（猎人机制版，已部署）

### 功能

| 功能 | 状态 |
|---|---|
| 手牌伤害预估（复用游戏 `DamageVar.UpdateCardPreview`） | ✅ |
| **保命优先**：不死 → 少掉血 → 最大伤害（字典序） | ✅ 已验证 |
| 怪物编号 1/2/3 + 计划里标明打哪只 | ✅ 已验证 |
| 斩杀提示（✅击杀 / ☠中毒先手击杀 / 残血 x→y） | ✅ 已验证 |
| 打死怪不再计入来袭 | ✅ |
| 虚弱 -25% 来袭、中毒先手击杀（敌方行动前） | ✅ 已验证（中和后意图 11→8） |
| **猎人卡池 91 张逐张语义**（`src/SilentLogic.cs`） | ✅ 已接 |
| 小刀生成 / 抽牌分流（`Cards` 变量重载修正） | ✅ 已接 |
| 小刀伤害 = 基础 + 力量 + 精准 | ✅ 已接 |
| 弃牌 + 弃牌触发（战术大师给能量、本能反应抽牌） | ✅ 已接 |
| 能量 / 敏捷 / 易伤 / 力量削减 | ✅ 已接 |
| 计算型伤害（终结技、飞镖、蜃景） | ✅ 已接 |
| 未建模的牌显式标注原因 | ✅ 已接 |

### 数据来源（重要）

- 猎人卡池转储：`docs/silent-pool.txt`（91 张，含**卡牌描述文本** + 机制变量）
- 卡牌语义交叉验证：CombatSolver 的 mirror 实现（`CardPileOnPlaySupport` / `CardChoiceSupport` 等）
- 关键结论：`Cards` 变量**被重载**——`刀刃之舞/Cards=3` 是生成 3 张小刀，`杂技/Cards=3` 是抽 3 张牌；
  `隐秘匕首/Cards=2` 是弃 2 张、`Shivs=2` 是生成 2 张小刀。

### 已知局限

- 小刀伤害用"基础+力量+精准"估算，**未计入易伤之外的其它修正**。
- 弃牌是确定性选择（优先弃可触发牌，其次弃期望值最低的），没有对"弃哪张"做完整搜索。
- 能力牌按"本回合无即时收益"处理（但准确/毒雾这类会跨回合生效）。
- X 费牌、需空抽牌堆的牌、以及 `SilentLogic.UnsupportedReason` 里列出的牌不参与推荐。
- 联机：只算自己，敌人意图按"对全队"的总伤害计算，因此预计掉血偏保守。

## 关键实现点（避免重复踩坑）

- UI 挂到 `NGame.Instance`；挂 `SceneTree.Root` 的延迟调用会落空。
- 动态加载的 Mod 程序集里 **Godot `_Ready`/`_Process` 虚函数不会被派发** → 显式 `EnsureStarted()` + `SceneTree.ProcessFrame` 信号驱动。
- 卡面效果**按变量类型匹配**（`BlockVar` / `PowerVar<PoisonPower>` / `CardsVar` / `RepeatVar`），不猜键名。
- `ModelDb` 在 Mod 初始化时还没建好 → 卡池转储放在**第一场战斗**时做。
- 直接跑 exe 会因缺 appID 导致 Steam 初始化失败 → **必须经 Steam 启动**。

## 构建 / 部署 / 测试循环

```powershell
Stop-Process -Name SlayTheSpire2 -Force            # 1. 关游戏（DLL 被占用）
pwsh -File "…\moddev\DamageAdvisor\build.ps1"       # 2. 构建 + 部署
Start-Process "steam://rungameid/2868840"           # 3. 经 Steam 启动
Get-Content "…\mods\DamageAdvisor\DamageAdvisor.log" # 4. 看日志
```

日志格式：`手牌快照#N … incoming=…` → 每张牌（含原始 `vars=[…]`）→ 每只怪意图 → 最终计划。

## 下一步候选

1. 用实机数据回归 v0.5 的猎人牌语义（刀刃之舞、杂技、弃牌触发等）。
2. 弃牌选择也纳入搜索（现在是指定策略）。
3. 能力牌的跨回合价值（毒雾、无尽刀刃、精准…）。
4. "允许掉血上限"设置项。
5. 面板可拖动/折叠。



.特殊 -and -not # DamageAdvisor 进度与状态

## v0.7.1（已修，待部署）

静态自查发现的 4 个问题，已修复：

1. **敌人格挡未计入**（P1）：`SimEnemy` 增加 `Block`，`ApplyDamage` 先扣格挡再扣血——之前遇到会加格挡的怪会高估伤害、误报"可斩杀"。
2. **弃牌触发的抽牌绕过抽牌副作用**（P1）：`本能反应` 被弃时的抽牌改走 `HandleDraws`，腐蚀波/速行者的抽牌副作用不再漏算，且尊循子弹时间的"本回合不能抽牌"。
3. **搜索去重键过粗**（P2）：去重条件补上 `Block/Poison/Shivs/Vulnerable/Weak`，避免把"同名但状态不同"的牌误判为重复。
4. **cfg 运行中不重载**（P3）：面板每 5 秒检查一次 `DamageAdvisor.cfg` 的修改时间，外部改配置即时生效（日志会打印"配置已重新加载"）。

同时修掉 3 个可空性警告（`TryGetValue` 的 out 参数），现在构建是 0 error / 0 warning。

### 仍未解决（已知）

- 多怪时伤害取数依赖游戏维护的单一 `PreviewValue`（对应当前/上次悬停目标），敌人易伤状态不对称时会有偏差。
- 面板拖动的坐标换算未实机验证。
- 联机不按队友分摊来袭；能力牌的跨回合价值不建模。

## v0.7（已编译，待部署）

### A. 允许掉血上限（面板交互）
- **F7** 循环切换 `0 → 1 → 2 → 3 → 5 → 0`（0 = 原来的严格保命）
- 存到 `mods\DamageAdvisor\DamageAdvisor.cfg`（`hpLossBudget=` / `collapsed=`），启动时读取
- 面板标题实时显示"允许掉血 ≤N"

### B. 跨回合视野（优先击杀）
- 评分顺序改为：**不死 → 掉血 ≤ 预算（超预算则少掉血优先）→ 优先击杀 → 最大伤害**
- 击杀的跨回合价值：打死一只怪 = 省掉它下一轮的攻击（同时本回合它的来袭也归零）

### C. 面板交互
- **鼠标拖动**面板任意位置可移动（轮询实现，不依赖 Godot 输入回调）
- **F10** 折叠/展开（折叠后只留"推荐"和"斩杀"两行）
- 按键总览：`F7 掉血上限` / `F8 隐藏` / `F9 注入测试牌` / `F10 折叠` / 拖动移动

## v0.6（已编译，待部署）

- X 费牌：串刺（8 伤 × 能量）、萎靡（X 力量削减 + X 虚弱）
- 子弹时间：手牌全部免费 + 本回合禁止抽牌
- 钢铁风暴：弃整手换等量小刀
- 计算下注：弃整手抽等量
- 精准：本回合小刀 +4
- 余像：每打一张牌 +1 格挡
- 暴露：施加 2 层易伤
- 修复：精确切击等计算型伤害读 CalculatedDamage；华丽收场真实检查抽牌堆；蜃景不再误标未建模
- F9 注入：手牌满 10 张时先弃最后一张


最后更新：2026-09-15 15:12

## 当前版本：v0.5（猎人机制版，已部署）

### 功能

| 功能 | 状态 |
|---|---|
| 手牌伤害预估（复用游戏 `DamageVar.UpdateCardPreview`） | ✅ |
| **保命优先**：不死 → 少掉血 → 最大伤害（字典序） | ✅ 已验证 |
| 怪物编号 1/2/3 + 计划里标明打哪只 | ✅ 已验证 |
| 斩杀提示（✅击杀 / ☠中毒先手击杀 / 残血 x→y） | ✅ 已验证 |
| 打死怪不再计入来袭 | ✅ |
| 虚弱 -25% 来袭、中毒先手击杀（敌方行动前） | ✅ 已验证（中和后意图 11→8） |
| **猎人卡池 91 张逐张语义**（`src/SilentLogic.cs`） | ✅ 已接 |
| 小刀生成 / 抽牌分流（`Cards` 变量重载修正） | ✅ 已接 |
| 小刀伤害 = 基础 + 力量 + 精准 | ✅ 已接 |
| 弃牌 + 弃牌触发（战术大师给能量、本能反应抽牌） | ✅ 已接 |
| 能量 / 敏捷 / 易伤 / 力量削减 | ✅ 已接 |
| 计算型伤害（终结技、飞镖、蜃景） | ✅ 已接 |
| 未建模的牌显式标注原因 | ✅ 已接 |

### 数据来源（重要）

- 猎人卡池转储：`docs/silent-pool.txt`（91 张，含**卡牌描述文本** + 机制变量）
- 卡牌语义交叉验证：CombatSolver 的 mirror 实现（`CardPileOnPlaySupport` / `CardChoiceSupport` 等）
- 关键结论：`Cards` 变量**被重载**——`刀刃之舞/Cards=3` 是生成 3 张小刀，`杂技/Cards=3` 是抽 3 张牌；
  `隐秘匕首/Cards=2` 是弃 2 张、`Shivs=2` 是生成 2 张小刀。

### 已知局限

- 小刀伤害用"基础+力量+精准"估算，**未计入易伤之外的其它修正**。
- 弃牌是确定性选择（优先弃可触发牌，其次弃期望值最低的），没有对"弃哪张"做完整搜索。
- 能力牌按"本回合无即时收益"处理（但准确/毒雾这类会跨回合生效）。
- X 费牌、需空抽牌堆的牌、以及 `SilentLogic.UnsupportedReason` 里列出的牌不参与推荐。
- 联机：只算自己，敌人意图按"对全队"的总伤害计算，因此预计掉血偏保守。

## 关键实现点（避免重复踩坑）

- UI 挂到 `NGame.Instance`；挂 `SceneTree.Root` 的延迟调用会落空。
- 动态加载的 Mod 程序集里 **Godot `_Ready`/`_Process` 虚函数不会被派发** → 显式 `EnsureStarted()` + `SceneTree.ProcessFrame` 信号驱动。
- 卡面效果**按变量类型匹配**（`BlockVar` / `PowerVar<PoisonPower>` / `CardsVar` / `RepeatVar`），不猜键名。
- `ModelDb` 在 Mod 初始化时还没建好 → 卡池转储放在**第一场战斗**时做。
- 直接跑 exe 会因缺 appID 导致 Steam 初始化失败 → **必须经 Steam 启动**。

## 构建 / 部署 / 测试循环

```powershell
Stop-Process -Name SlayTheSpire2 -Force            # 1. 关游戏（DLL 被占用）
pwsh -File "…\moddev\DamageAdvisor\build.ps1"       # 2. 构建 + 部署
Start-Process "steam://rungameid/2868840"           # 3. 经 Steam 启动
Get-Content "…\mods\DamageAdvisor\DamageAdvisor.log" # 4. 看日志
```

日志格式：`手牌快照#N … incoming=…` → 每张牌（含原始 `vars=[…]`）→ 每只怪意图 → 最终计划。

## 下一步候选

1. 用实机数据回归 v0.5 的猎人牌语义（刀刃之舞、杂技、弃牌触发等）。
2. 弃牌选择也纳入搜索（现在是指定策略）。
3. 能力牌的跨回合价值（毒雾、无尽刀刃、精准…）。
4. "允许掉血上限"设置项。
5. 面板可拖动/折叠。



.已点名 } | Format-Table 类,名,类型
```

### 仍未解决

- 多怪时伤害取数依赖单一 `PreviewValue`（对应当前/上次悬停目标），易伤状态不对称时有偏差。
- 谋杀（Murder）的"本场每抽一张 +1"依赖游戏 `CalculatedDamage` 预览；预览为 0 时会退回基础值（偏保守）。
- 触媒（Accelerant，中毒额外触发）未建模，目前按"本回合无即时收益"处理。
- 面板拖动坐标未实机验证；联机不按队友分摊来袭。

## v0.7.1（已修，待部署）

静态自查发现的 4 个问题，已修复：

1. **敌人格挡未计入**（P1）：`SimEnemy` 增加 `Block`，`ApplyDamage` 先扣格挡再扣血——之前遇到会加格挡的怪会高估伤害、误报"可斩杀"。
2. **弃牌触发的抽牌绕过抽牌副作用**（P1）：`本能反应` 被弃时的抽牌改走 `HandleDraws`，腐蚀波/速行者的抽牌副作用不再漏算，且尊循子弹时间的"本回合不能抽牌"。
3. **搜索去重键过粗**（P2）：去重条件补上 `Block/Poison/Shivs/Vulnerable/Weak`，避免把"同名但状态不同"的牌误判为重复。
4. **cfg 运行中不重载**（P3）：面板每 5 秒检查一次 `DamageAdvisor.cfg` 的修改时间，外部改配置即时生效（日志会打印"配置已重新加载"）。

同时修掉 3 个可空性警告（`TryGetValue` 的 out 参数），现在构建是 0 error / 0 warning。

### 仍未解决（已知）

- 多怪时伤害取数依赖游戏维护的单一 `PreviewValue`（对应当前/上次悬停目标），敌人易伤状态不对称时会有偏差。
- 面板拖动的坐标换算未实机验证。
- 联机不按队友分摊来袭；能力牌的跨回合价值不建模。

## v0.7（已编译，待部署）

### A. 允许掉血上限（面板交互）
- **F7** 循环切换 `0 → 1 → 2 → 3 → 5 → 0`（0 = 原来的严格保命）
- 存到 `mods\DamageAdvisor\DamageAdvisor.cfg`（`hpLossBudget=` / `collapsed=`），启动时读取
- 面板标题实时显示"允许掉血 ≤N"

### B. 跨回合视野（优先击杀）
- 评分顺序改为：**不死 → 掉血 ≤ 预算（超预算则少掉血优先）→ 优先击杀 → 最大伤害**
- 击杀的跨回合价值：打死一只怪 = 省掉它下一轮的攻击（同时本回合它的来袭也归零）

### C. 面板交互
- **鼠标拖动**面板任意位置可移动（轮询实现，不依赖 Godot 输入回调）
- **F10** 折叠/展开（折叠后只留"推荐"和"斩杀"两行）
- 按键总览：`F7 掉血上限` / `F8 隐藏` / `F9 注入测试牌` / `F10 折叠` / 拖动移动

## v0.6（已编译，待部署）

- X 费牌：串刺（8 伤 × 能量）、萎靡（X 力量削减 + X 虚弱）
- 子弹时间：手牌全部免费 + 本回合禁止抽牌
- 钢铁风暴：弃整手换等量小刀
- 计算下注：弃整手抽等量
- 精准：本回合小刀 +4
- 余像：每打一张牌 +1 格挡
- 暴露：施加 2 层易伤
- 修复：精确切击等计算型伤害读 CalculatedDamage；华丽收场真实检查抽牌堆；蜃景不再误标未建模
- F9 注入：手牌满 10 张时先弃最后一张


最后更新：2026-09-15 15:12

## 当前版本：v0.5（猎人机制版，已部署）

### 功能

| 功能 | 状态 |
|---|---|
| 手牌伤害预估（复用游戏 `DamageVar.UpdateCardPreview`） | ✅ |
| **保命优先**：不死 → 少掉血 → 最大伤害（字典序） | ✅ 已验证 |
| 怪物编号 1/2/3 + 计划里标明打哪只 | ✅ 已验证 |
| 斩杀提示（✅击杀 / ☠中毒先手击杀 / 残血 x→y） | ✅ 已验证 |
| 打死怪不再计入来袭 | ✅ |
| 虚弱 -25% 来袭、中毒先手击杀（敌方行动前） | ✅ 已验证（中和后意图 11→8） |
| **猎人卡池 91 张逐张语义**（`src/SilentLogic.cs`） | ✅ 已接 |
| 小刀生成 / 抽牌分流（`Cards` 变量重载修正） | ✅ 已接 |
| 小刀伤害 = 基础 + 力量 + 精准 | ✅ 已接 |
| 弃牌 + 弃牌触发（战术大师给能量、本能反应抽牌） | ✅ 已接 |
| 能量 / 敏捷 / 易伤 / 力量削减 | ✅ 已接 |
| 计算型伤害（终结技、飞镖、蜃景） | ✅ 已接 |
| 未建模的牌显式标注原因 | ✅ 已接 |

### 数据来源（重要）

- 猎人卡池转储：`docs/silent-pool.txt`（91 张，含**卡牌描述文本** + 机制变量）
- 卡牌语义交叉验证：CombatSolver 的 mirror 实现（`CardPileOnPlaySupport` / `CardChoiceSupport` 等）
- 关键结论：`Cards` 变量**被重载**——`刀刃之舞/Cards=3` 是生成 3 张小刀，`杂技/Cards=3` 是抽 3 张牌；
  `隐秘匕首/Cards=2` 是弃 2 张、`Shivs=2` 是生成 2 张小刀。

### 已知局限

- 小刀伤害用"基础+力量+精准"估算，**未计入易伤之外的其它修正**。
- 弃牌是确定性选择（优先弃可触发牌，其次弃期望值最低的），没有对"弃哪张"做完整搜索。
- 能力牌按"本回合无即时收益"处理（但准确/毒雾这类会跨回合生效）。
- X 费牌、需空抽牌堆的牌、以及 `SilentLogic.UnsupportedReason` 里列出的牌不参与推荐。
- 联机：只算自己，敌人意图按"对全队"的总伤害计算，因此预计掉血偏保守。

## 关键实现点（避免重复踩坑）

- UI 挂到 `NGame.Instance`；挂 `SceneTree.Root` 的延迟调用会落空。
- 动态加载的 Mod 程序集里 **Godot `_Ready`/`_Process` 虚函数不会被派发** → 显式 `EnsureStarted()` + `SceneTree.ProcessFrame` 信号驱动。
- 卡面效果**按变量类型匹配**（`BlockVar` / `PowerVar<PoisonPower>` / `CardsVar` / `RepeatVar`），不猜键名。
- `ModelDb` 在 Mod 初始化时还没建好 → 卡池转储放在**第一场战斗**时做。
- 直接跑 exe 会因缺 appID 导致 Steam 初始化失败 → **必须经 Steam 启动**。

## 构建 / 部署 / 测试循环

```powershell
Stop-Process -Name SlayTheSpire2 -Force            # 1. 关游戏（DLL 被占用）
pwsh -File "…\moddev\DamageAdvisor\build.ps1"       # 2. 构建 + 部署
Start-Process "steam://rungameid/2868840"           # 3. 经 Steam 启动
Get-Content "…\mods\DamageAdvisor\DamageAdvisor.log" # 4. 看日志
```

日志格式：`手牌快照#N … incoming=…` → 每张牌（含原始 `vars=[…]`）→ 每只怪意图 → 最终计划。

## 下一步候选

1. 用实机数据回归 v0.5 的猎人牌语义（刀刃之舞、杂技、弃牌触发等）。
2. 弃牌选择也纳入搜索（现在是指定策略）。
3. 能力牌的跨回合价值（毒雾、无尽刀刃、精准…）。
4. "允许掉血上限"设置项。
5. 面板可拖动/折叠。







