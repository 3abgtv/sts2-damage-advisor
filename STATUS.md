# DamageAdvisor 进度与状态

## engine/rewrite 分支进度（2026-09-20）

独立分支，做"克隆 + 镜像引擎"的新架构（`master` 上的只读模型保持不动）。多人下**新引擎完全不介入**，
继续用 `master` 的模型（多人降级）。

**第一阶段：捕获 + 克隆 —— 通过 ✓**（四项硬判据，31+ 次实机探测）

| 判据 | 结果 |
|---|---|
| 捕获零影响 | ✓ 前后真机指纹逐字一致 |
| 克隆体隔离（卡牌） | ✓ `CreateCloneForPlayer` 是深拷贝（`DynamicVars`/`EnergyCost` 不共享） |
| 克隆体隔离（遗物） | ✓ 反射 `MemberwiseClone → DeepCloneFields → AfterCloned`（`ToMutable()` 不行：可变模型有守卫） |
| RNG 不推进 | ✓ 取快照前后 `ToSerializable()` 一致 |
| 开销 | ✓ <1ms |

**第二阶段第一刀：影子状态 + 命令镜像 + 差分验证 —— 闭环成立 ✓**

- `SimState`（只装捕获期抄出来的纯值，不持游戏对象）+ `SimCommands`（伤害先扣格挡再扣血 / 格挡 / 能量 / 打出一张牌）
- F5 探测：在影子上打一张单体攻击牌 → 给出预测 → 之后每次面板刷新自动核对（不用掐时机按 F5）
  （**已被下面的"整套计划重放"取代**：单张牌扩成了主模型的完整推荐计划）
- **第二阶段第二步：能力牌镜像（余像/精准/涂毒/腐蚀波/群蛇形态/速行者）—— 实机确认 ✓**
  ```
  [差分] ✓ 通过：实机与预测一致（第 1 回合打「余像」→ 幽灵船 …、我 1能量/9格挡）—— 差分验证链闭环
  ```
  六项全等（敌血/中毒/易伤/虚弱 + 我的能量/格挡）。

**第三阶段：与面板对接 —— 计划重放比对（每回合自动跑）**

主模型的推荐计划会被喂给新引擎重放一遍，面板开发版提示行并排显示两边数字：

```
新引擎重放推荐：伤害 12 / 格挡 +10 / 掉血 3 / 剩 0 能量　（主模型：伤害 12 / 格挡 +10 / 掉血 3）
```

不一致的地方就是其中一边的 bug —— 这是"替代主模型"之前必须清掉的东西。

**F5 ＝ 重放整套推荐计划 + 终态指纹闭环**（原来只挑一张牌，面板上看不到完整顺序）

- 面板给出**完整顺序**（牌名 → 牌名 → …）＋预期终态；待核对的预测从"单张牌的六项值"
  升级为**终态指纹**：`回合 | 每只存活敌人的 血·中毒·易伤·虚弱 | 我的 能量·格挡`
- 判定：回合一变即作废（说明已结束回合）；指纹**逐字相等**即通过，并把结论顶到面板上
  （`✓ 已核对一致（…）`），不用翻日志；否则保留待核对，等下一次面板刷新
- 记下出牌前的实机指纹，挡掉"还没开始打"那一帧的噪声日志
- 打到一半**不报错**：3 张里打完第 1 张本来就不等于终态，面板继续显示顺序提示，中间态只进日志
- 顺序里**带目标**（`打击[1号]`，与面板计划同一写法）；计划含生成牌时**不登记预测**，
  面板直接说明 `[N 张生成牌无法重放 → 本回合不自动核对]` —— 影子手里没有小刀，
  硬比会把"重放不了"误报成"算错了"
  （**仅第三阶段如此**：那时是"重放主模型的计划"，牌得先在影子里存在才放得出。
  第四阶段起登记的是搜索自己的终态，影子是自己生成小刀的，这条限制随之消失 —— 见下文）

**F5 闭环实机验证 —— 2/2 通过 ✓**（2026-09-20，单怪战斗）

| 回合 | 面板给的顺序 | 预测终态 | 实机 |
|---|---|---|---|
| 1 | 打击[1号] → 计算下注 → 打击[1号] → 打击[1号] | `r1\|幽灵船:45/0/0/0\|m:0/0` | ✓ 逐字一致 |
| 2 | 防御 → 中和[1号] → 投掷匕首[1号] → 防御 | `r2\|幽灵船:37/0/0/1\|m:0/10` | ✓ 逐字一致 |

- 中间态只进日志不报警（63→57→51→45，每张打击 -6）—— 设计如此，不是错误
- **顺带验证了"伤害取值走游戏预览"**：中和 `Damage=3/2.3`、投掷匕首 `Damage=9/6.8`（都被削 25%），
  计划写的 `伤害=8` 正是 2+6，实机也是 8。**引擎若写死基础值这里就会差 4 点、直接不通过**
- 覆盖范围**窄**：只碰了 打击/防御/中和/投掷匕首/计算下注 ＋ 耗能·格挡累计·易伤虚弱施加·抽弃牌；
  **未覆盖** 中毒结算 / 小刀生成 / 能力牌 / 多怪 / 易伤增伤
- `[N 张生成牌无法重放]` 这条分支在第三阶段**未被触发过**；第四阶段起这条路已不走重放，
  所以它不再是待验项 —— 含刀刃之舞的回合现在直接由搜索终态核对（见下文）

**第四阶段：新引擎自己的搜索（`SimSearch`）—— 已落地，待实机**

以前它只会重放主模型的计划；现在它在影子状态上**自己搜**：

- 骨架照主模型抄：状态指纹去重 + 同名牌剪枝 + 逐目标枚举 + 每个节点都 Consider（任意前缀都是候选）
- 打分**共用** `DamageModel.IsBetter`（外层加了个转发）—— 分数口径必须两边一致，
  否则差分里会多出一个"偏好不同"的假差异，把真正的规则差异淹掉
- `SimState.Clone()` 是必需的地基：`SimCommands` 就地改状态，不克隆就会把兄弟分支互相污染
- 搜索返回**它自己走到的终态**，直接登记预测 —— **不再需要重放**。
  于是"计划含生成牌（小刀等）"也能核对了（影子自己生成了小刀），
  旧路径那条 `[N 张生成牌无法重放]` 的限制在这条路上不存在
- 未镜像语义记在 `SimSearch.Gaps` 台账（判据写在 `CardEffect` 字段上，不是牌名清单）：
  命中就标"不可比"，而不是给玩家一个假的差异；**每在 `SimCommands` 补上一条就删掉台账里一条**
- 捕获敌人来袭伤害时**不抄** `advice.EnemiesBefore`：那份列表就是搜索自己就地改的那一份，
  搜索跑完后已经带着"某条分支上打过的削力量/虚弱"，抄过来会低估掉血。改为从活体
  `DamageModel.IncomingOf` 现读，`StrengthLoss`/`WeakThisTurn` 恒置 0（意图里已经算过，不重复扣）

F5 现在的输出（面板）：

```
新引擎：打击[1号] → 防御　伤害 6 / 格挡 +5 / 掉血 0（N 状态）
主模型：防御 → 打击[1号]　伤害 6 / 格挡 +5 / 掉血 0
✓ 两边方案一致
```

- 动手前先用临时自检台（手搭 `SimState`，不碰游戏对象）跑了 13 项，全过：
  格挡先扣 / 能量不足只打一张 / 同名剪枝（6 张 0 费只展开 7 个状态）/ 中毒先手击杀不计掉血 /
  无实体按段数折算 / 终态与计划同源 / 空手牌 / 认得出未镜像语义
- **待实机**：打一回合按 F5，看两边计划与数字；两边不一致时判断是"机制差异"还是"计划选择差异"

**新引擎当前的覆盖（相对主模型）**

| | 状态 |
|---|---|
| 伤害（先扣格挡）/ 格挡 / 能量 / 状态（中毒·虚弱·易伤） | ✓ |
| 抽牌（含洗牌边界）/ 弃牌 / 奇巧自动打出 / 生成小刀 / X 费 / 爆发 | ✓ |
| 能力：余像·精准·涂毒·腐蚀波·群蛇形态·速行者·融入暗影·子弹时间 | ✓ |
| 紧勒 / 暴露 / 刀扇 / 手上技法 / 触媒 | ✓ |
| **新引擎自己的搜索**（`SimSearch`：`SimState` 上 DFS + 共用打分 + 克隆分叉） | ✓ 自检 13 项，待实机 |
| 跨回合（`EndTurn`）/ 怪物 AI | ✗ 未做 |

**差分对照抓出的真实规则差异（游戏有我模型没有/写错的）**

| 差异 | 证据 | 修法 |
|---|---|---|
| **融入暗影的格挡 ×2** | 预测 8 格挡 / 实机 14（6×2 + 1×2） | `GainBlock` 在 DoubleBlock 时翻倍；捕获读 `ShadowmeldPower` |
| **"每打出一张牌"类触发不给自己触发** | 打出余像时格挡仍 0（我模型给了 +1） | 触发挪到"能力累加"之前，用**打牌前**的层数（主模型本来就是对的，只有新引擎写错） |

> **后续怎么做见 [`docs/engine-rewrite-roadmap.md`](docs/engine-rewrite-roadmap.md)**：
> 第 4 步加搜索 → 第 5 步怪物 AI（需先验"读它不消耗 RNG"）→ 第 6 步跨回合 → 第 7 步完整性（遗物/药水/洗牌/切换）。

## 2026-09-20 v0.9.9 — 覆盖率补齐：显性 2 张 + 隐性 4 处

"覆盖率"分两层：面板上标着"未建模"的（显性），和"不说未建模、但也没算"的第二效果（隐性）。这轮两层都清。

### 显性（面板上"未建模"字样清掉）

| 牌 | 以前 | 现在 |
|---|---|---|
| **刀刃陷阱** | 未建模（"需要消耗堆里的小刀"） | **支持**：伤害 = 卡面 `CalculatedShivs`（打几张）× 小刀伤害（小刀伤害沿用"优先取真实小刀预览值"那套） |
| **爆发** | 未建模（"技能双触发"） | **支持**：本回合接下来 N 张技能牌载荷翻倍（见下方假设） |
| **夜魇** | 未建模（"复制手牌"） | 备注改为"**下个回合**才加入复制的牌，本回合无收益"——它确实没效果，不该叫未建模 |
| **调制/夹击/消影/鬼祟** | 未建模 | 备注统一为"**多人向**，单人局无效果"——单人局不建模本来就是**正确**的 |

### 隐性（变量里看不到的第二效果）

| 牌 | 描述里的第二效果 | 现在 |
|---|---|---|
| **精密瞄准** | 本回合每打出过一张技能牌，耗能减 1 | 不用逐张建模：`SafeCost` 改走 **`CardEnergyCost.GetResolved()`**（含全部修正的当前费用），遗物/其它牌的费用修正也一并覆盖 |
| **手上技法** | 给手牌中一张技能牌添加奇巧 | 支持：模型替你挑"弃掉最划算"的那张（复用弃牌评分 `SlyValue`）；为此把 `CardEffect` 改成 **record**，才能 `with { IsSly = true }` 复制一份带标记的实例（手牌对象在各搜索状态间共享，不能就地改） |
| **触媒** | 中毒额外触发 1 次 | 支持：影响"中毒先手击杀"判定，总量按 `stack + (stack-1) + …`（**假设**：每次触发后层数 -1；待实机验证） |
| **弹跳药瓶 / 连续反弹** | 随机目标 | 保持"全部次数集中在同一目标"，但在面板与日志标注"**随机目标**"（打完后面板会重读真实状态，见下） |

### 顺手修的一个刷新缺口

刷新指纹以前只比敌人的**血量/格挡**，不算 debuff —— 如果某次只有中毒层数变了（手牌、能量都没变），面板会停在旧建议上。
现在 `DebuffKey`（毒/虚弱/易伤/力量）也进指纹。

### 爆发的实现假设（待实机确认）

采用"**载荷翻倍**"而不是重构 `PlayCard` 重复整个结算：伤害/格挡/中毒/虚弱/易伤/抽牌/小刀/能量/自身能力值 ×2，
但**不重复"每打出一张牌"类触发**（余像每张牌 +1 格挡、群蛇形态每张牌随机伤害、紧勒每张牌掉血）——
后者的语义是"打出行为"而非"结算内容"。连"毒性爆发立即触发"这类**属于卡牌自身**的效果则触发两次。

**验证状态**：编译 0 warning / 0 error（开发版与工坊版）；`verify-log.py` 全量仍是那 52 处历史不一致（无新增回归）。
新机制待实机验证：刀刃陷阱、爆发、手上技法、触媒、精密瞄准（费用）——已加进 F9 清单。
`verify-log.py` 的白名单同步加了"刀刃陷阱跳过伤害比对"（它的伤害卡面里没有可推的变量）。

### 代码推演又挖出并修掉的 4 处

用"逐张读描述 + 走代码"的方式把这 91 张牌过了一遍（`tools/scan-cards.py` 先把含"触发/条件/次数"关键词的 37 张捞出来），
又发现 4 处偏差，一并修掉：

| # | 牌 | 偏差 | 修法 |
|---|---|---|---|
| 1 | **涂毒 Envenom** | 描述是"每有**一次**攻击造成未格挡伤害 +1 毒"（**每段**），代码是**每张牌**只加一次 → 匕首雨(2 段)/连续反弹(4 段) 的毒被低估 | `ApplyEnvenom` 按 `played.Hits` 叠；奇巧自动打出的攻击也顺带补上了涂毒（以前完全没算） |
| 2 | **逃脱计划 EscapePlan** | "**如果抽到的是技能牌**才 +3 格挡"被**无条件**给了 → 保命侧高估 3 点 | 新增 `BlockIfSkillDrawn`：出牌时按**实际抽到的牌**判定（抽牌堆顺序在搜索里是已知的，能算准）；`HandleDraws` 改为返回这批抽到的牌 |
| 3 | **爆发 × 刀刃陷阱** | 刀刃陷阱是技能牌 → 被翻倍 → 模型按"消耗堆的小刀再打一遍"算，但第一次打出后消耗堆已空 | 刀刃陷阱的伤害改成在 `damage *= times` **之后**赋值（不参与翻倍） |
| 4 | **刀刃陷阱（升级版）** | 打出的是**升级后**的小刀，模型按普通小刀算 → 每张低估 2 点 | `Solve` 新增 `exhaustPile` 参数：从**消耗堆**里取真实小刀的卡面值（升级版的小刀就在那里），取不到才退回其它牌堆/手算 |

另外**排查过、确认没问题**的：终结技/飞镖/铭记死亡/谋杀/精确切击的计算型伤害都走游戏的 `CalculatedDamage` 实时预览；
华丽收场的"抽牌堆为空"限制、子弹时间、暗影步、钢铁风暴、腐蚀波、速行者、幻影之刃、尖啸、融入暗影 与描述一致。

> ⚠️ 待实机确认的新增项：**涂毒 × 多段攻击**（涂毒生效时打匕首雨，中毒应 +2 而不是 +1）；
> **刀刃陷阱升级版**（伤害应等于"张数 × 升级小刀伤害"）；以及这轮改动里影响面最大的
> **费用取 `GetResolved()`**（找一张被减费的牌，核对面板费用与游戏是否一致）。

## 待办（2026-09-20）

### 1. 实机验证（做完才好更新工坊）

- [x] **F5 闭环（第三阶段：重放对照）**：2/2 通过，覆盖范围与结论见上方「F5 闭环实机验证」。
      原计划里还剩一条分支没走过（打到含生成牌的回合应显示 `[N 张生成牌无法重放]`）——
      **第四阶段起该分支已不再出现**（登记的是搜索自己的终态，不再是重放），不必再验。
- [ ] **第四阶段实机（新引擎自己的搜索）**：打一回合按 F5，看面板两行（`新引擎：…` / `主模型：…`）。
      预期：两边计划常常一致；若不一致，两边数字都要看 —— 判断是**机制差异**（某状态/某张牌算错，
      回去修 `SimCommands` 的镜像）还是**计划选择差异**（机制都对、只是搜得不一样，那才是骨架的问题）。
      照"新引擎"那行打完，面板应出 `✓ 已核对一致（…）`。
      重点试**含刀刃之舞的回合**（这条以前根本没法核对）。
- [ ] **跟踪语义（关键）**：F9 注入「跟踪」→ 打出 → 结束回合 → **下一回合看面板攻击数字是否变大**（如打击原 6）。
      变大 = 游戏卡面预览值已含跟踪，当前实现正确；**没变 = 要改成"按解算时身上有跟踪"加 ×1.5**
      （`DamageMultiplier` 里把 `state.TrackingNew` 换成解算时读到的跟踪状态）。
- [ ] 新建模的 5 张：幽魂形态（"合计来袭"应塌成每段 1 点）、紧勒（之后每张牌让目标掉 2、无视格挡）、
      猛扑（下一张技能 0 费且真能打出）、咕嘟冒泡（无毒目标不加毒）、回响斩击（多怪击杀后重复全体伤害）。
- [ ] 格挡修复复验：打一场"本场敏捷 > 0"的战斗（第 1 回合出灵动步法 → 第 2 回合看防御是否显示 5+敏捷），
      然后 `python tools/verify-log.py --tail 3000`，应**无 block 不一致**。
- [ ] v0.9.7 那 6 张：侧步（+0 能量）、匕首雨（dmg=8）、暴露（清格挡）、刀扇（小刀打全体）、
      逃脱计划（draw=1）、终结技（先出一张攻击再出，应算 2 次）。
- [ ] 覆盖率这轮新加的 5 处：刀刃陷阱（伤害=张数×小刀）、爆发（下一张技能效果翻倍）、
      手上技法（手牌里某张技能牌出现 [奇巧]）、触媒（**9 层毒打 10 血怪应正好毒死**，验证毒结算假设）、
      精密瞄准（打出技能牌后它的费用显示变小）。

### 1.5 播种"开局已在场的能力"（2026-09-24）

已落地：`LivePowerSeed`（一处读取、两个引擎共用）—— 主模型 `initialState` 补 10 项、
影子改成从 advice 取、并**删掉影子那处 `ShivBonus` 重复计算**。自检台 ⑯–㉑ 钉住（31/31）。

> 这个 bug 是**双向**的：主模型一项都没播种（余像整段丢失，实机三次证明它低估格挡 3 点），
> 影子则**播多了**（`ShivBonus` 等于把精准算两遍 —— `BuildShivDamage` 里已含 accuracy）。
> 所以"两边各自读 live"这个结构本身就是 bug 源，收敛成一处读取才是修法。

- [ ] **金标实测三条（别猜）**：
      ① **`ShivBonus` 在不在小刀伤害里** —— 现在按"在"处理（只信模板值）。验法：场上已有精准时看小刀卡面
         数字是否 = 基础+精准。**是**则当前正确；**否**则要改回去（播种 + 模板不含精准）。
         `FirstShivBonus`（幻影之刃 +9）同理一起看。
      ② ~~**`DoubleBlock`（融入暗影）**~~ **已定案（2026-09-25 实机）**：融入暗影**已在场**时，
         卡面格挡预览**已经**是翻倍后的值 —— 影子不能再翻一次。证据：打出融入暗影后按 F5，
         新引擎给「防御 → 格挡 +20」而实机是 **10**（= 5×2，只翻一次）；主模型的 +16 与实机一致。
         所以 `SimState.Capture` **不再播种 `DoubleBlock`**（恒 false）；**计划内**打出仍翻倍
         （卡面是捕获前读的），那条路径由 `PlayCard` 里 `card.DoubleBlock → sim.DoubleBlock = true` 负责。
         自检台 ㉕㉖ 钉住两个方向。**这条是差分自己抓出来的** —— 我前一天还判断"保持从 live 读"，
         是错的一方。
      ③ **`TrackingNew` 与"易伤"是同一个门，必须同进同退** —— 见上面「跟踪语义」那条实验。
- [ ] **顺带挖出的两个真 bug**（与播种无关，但播种会把它们放大）：
      · **紧勒的叠加已修**（2026-09-25）：游戏里是敌人身上的 `PowerStackType.Counter`
        （官方定义 "Amount is visible, and must be manually incremented"），`StranglePower` 的文档
        也说要处理 "especially when stacking" → 是**相加**。以前主模型取 max、影子直接覆盖，
        两张紧勒都只算 2。两处都改成相加，自检台 ㉒ 钉住（8+2+8=18、打完层数 4）。
        **已知限制**：单槽模型 —— 换目标后旧目标的紧勒不再跟踪（要真做对得把它挪到 `SimFoe`/`SimEnemy`
        上按敌人存，与中毒/虚弱一样）。**待实机**：连打两张紧勒，看"每张牌失去"是否 2→4。
      · **新引擎涂毒两处偏差已对齐**（2026-09-25）：① 速行者那条被动伤害路径以前也会触发涂毒
        （涂毒只认**攻击**造成的伤害，主模型只在卡牌伤害路径触发）—— 删掉；② 以前按"张"叠，
        主模型是 `state.Envenom * Math.Max(1, hits)` 按**段数**叠 —— 两处触发点都补上段数。
        自检台 ㉓㉔ 钉住（2 段 × 3 = 6；速行者 5 伤但毒仍为 0）。

### 2. 工坊（物品 id 3804446078）

- [ ] **网页端把可见性改成 Unlisted/Public**（`workshop.json` 里的 `visibility` 首次上传不生效、重传也没用；
      自测方法：未登录浏览器窗口能否打开物品链接）。
- [ ] 验证工坊版能加载：第二账号订阅，或手动把 `workshop/content/` 拷到
      `steamapps/workshop/content/2868840/3804446078/`（同时把本地 `mods/DamageAdvisor` 挪开）。
- [ ] 发 0.9.9：`powershell -File build.ps1 -Workshop` → `ModUploader.exe upload -w …\workshop`（同一物品 id）。
- [ ] 封面换成实机截图（现在 `workshop/image.png` 是占位图）。

### 3. 下一轮（原计划的 ③ 结构性缺口）

- [ ] 抽牌堆空时不洗弃牌堆（后期抽牌类牌收益被吞）。
- [ ] 伤害只取"单个采样目标"的卡面预览（多目标时是近似值，面板脚注已注明）。
- [ ] **跨回合第二层**：给下回合收益**估值并纳入本回合决策**（让毒雾/无尽刀刃/计划妥当这类
      "本回合无收益、长期有利"的牌能被推荐）——资源账的数据结构已经就位（`NextTurnResources`）。
- [ ] **跨回合第三层（敌人）**：用 `MoveStateMachine` 推敌人下一手。⚠️ 需先确认"读它不会消耗 RNG"
      （`RandomBranchState` 带权重）；脚本化的怪能推准，随机分支只能按最坏情况/期望。
- [ ] 实证三条跨回合假设：基础抽牌是不是 5 张；残影是否"保留未消耗的格挡"；计划妥当是否"整手牌留下"。
- [ ] 剩余未建模：调制/夹击/消影/鬼祟（多人向，单人局无效果，**已如实标注**）、
      谋划专家（触发方式待实机确认）、墨影小刀（读不到数值）、狩猎的卡牌奖励（战斗结算时）、
      毒雾/残影/计划妥当/无尽刀刃/必备工具等纯跨回合能力。
- [ ] 随机目标的两张（弹跳药瓶/连续反弹）现在是"全部次数集中在一个目标"，若要更准可考虑
      读游戏 RNG 做真实预测（RandomForeseer 那条路），代价大、且会改变 mod 定位。

## 2026-09-20 v0.9.9 — 跨回合第一层：下回合资源账

跨回合**分三层**做。第一层只算"下回合**确定**能拿到什么"，**不改变本回合推荐**；
第二层（给下回合收益估值、纳入决策）与第三层（真·两回合搜索）暂不做。

面板新增一行（折叠时隐藏）：

```
推荐：侧步 → 防御 → 打击[1号]
下回合：现在结束 → 3能量/5张/0格挡
          照推荐打 → 4能量/5张/0格挡
```

**算哪些**（"确定"是判据：抽牌堆顺序、保留效果、下回合生效的牌都算；洗牌抽到什么、敌人换招不算）：

| 项 | 来源 |
|---|---|
| 下回合能量 | 能量上限 + 侧步（下回合 +1） |
| 下回合手牌 | 下回合抽牌（受洗牌下限限制）+ 保留的牌 + 猎杀者(+2)/必备工具(+1)/无尽刀刃(+1)/夜魇(+3) |
| 下回合格挡 | 残影保留的"没被打掉的部分" + 闪躲翻滚(+4) |

**两条判定规则**：① 牌类效果只有"**本回合打出的**"才计入下回合（上回合打的猎杀者已经在本回合抽到手了）；
② 能力类（残影/计划妥当/无尽刀刃/必备工具）读玩家身上**是否已生效**，与本回合打出取**或**、不重复计。
③ 洗牌按**可用下限**算：`min(5, 抽牌堆 + 弃牌堆)`。

**代码推演自检**（写入代码前先按代码走了一遍）：侧步 → 能量 3→4 ✓；残影+防御 对来袭 6 → 保留 4 格挡 ✓。

> ⚠️ **待实机验证的三条假设**：① **基础抽 5 张是写死的**（游戏若有改基础抽牌的效果会偏）；
> ② 残影按"未消耗的格挡"算、计划妥当按"回合结束不弃手牌"算（都从描述推的）；
> ③ 敌人下回合威胁**没做**（推它要碰可能消耗 RNG 的 `MoveStateMachine` API，单独一轮）。

## 2026-09-20 v0.9.9 — ② 静默低估补齐：6 张牌 + 2 处如实标注

"未建模"分三类，这次处理最危险的一类：**面板看着有支持、其实没算**（会给出错误自信的建议）。

| 牌 | 以前 | 现在 |
|---|---|---|
| 跟踪 Tracking | 标"能力牌，本回合无收益"**且被排除出搜索** | 支持：打出后本回合对**虚弱**敌人的攻击 ×1.5（只算计划中新打出的，见下） |
| 幽魂形态 WraithForm | 完全没算 | 支持：获得无实体 → 每段来袭伤害按 **1 点**折算（搜索与面板"来袭"同步改） |
| 紧勒 Strangle | 只算了那 8 点伤害 | 支持：之后每打出一张牌，该敌人失去 2 点生命（无视格挡，计入伤害与击杀） |
| 猛扑 Pounce | 只算了那 14 点伤害 | 支持：下一张技能牌耗能变 0（搜索里真的免费，且打出技能后消耗掉） |
| 咕嘟冒泡 BubbleBubble | 无条件给 9 层毒 | 支持：目标身上没有中毒时不给毒 |
| 回响斩击 EchoingSlash | 只算一次全体伤害 | 支持：每击杀一名敌人就重复一次全体伤害（连锁结算） |
| 墨之刃 BladeOfInk | 按普通小刀算 | **仍按普通小刀算**：墨影小刀不是独立卡（Shiv 令牌 + 墨影附魔），读不到数值 → 面板明确标注"可能偏低" |
| 谋划专家 MasterPlanner | 标"能力牌，本回合无收益" | 改为**未建模**：描述是"当你打出技能牌时，该牌获得奇巧"，触发方式未确认，不猜 |

**跟踪的重复计算风险（重要）**：`card-semantics-change` skill 里记着"卡面预览值已含力量/虚弱/易伤修正"，
跟踪是同类全局加成，所以**本场早就有的跟踪很可能已经在预览值里**。因此只给"本回合计划中新打出的跟踪"
×1.5（与 `VulnerableThisTurn` 同一个思路），不按"玩家身上有无跟踪"加。

**验证状态**：编译 0 warning / 0 error（开发版与工坊版都过）；`verify-log.py` 全量仍是那 52 处历史不一致
（无新增回归）、最后 300 行 0 不一致。**这批新机制全部待实机验证** —— 那 6 张牌在现有日志里一次都没抽到过。

## 2026-09-19 工具：日志自动比对 `tools/verify-log.py`

把"改完这张牌到底对不对"从人工核对变成可重复执行的一步。

- **做什么（A 类）**：解析 `DamageAdvisor.log` 里每条牌行（`牌 … dmg=.. block=.. poison=.. draw=.. shivs=..`）
  与同一行 `vars=[...]` 里游戏自己的卡面值对照，输出不一致清单到 `docs/verify-report.txt`（该文件已 gitignore，
  含游戏内文本）。非零退出码 = 有不一致，可用于拦住提交/发布。
- **例外表**（脚本头部，都是"故意的转换"）：投掷匕首/逃脱计划（描述里写死的抽 1）、匕首雨（写死的"两次"）、
  刀刃之舞/斗篷与匕首/袖里乾坤/墨之刃（`Cards=` 是生成小刀数）、隐秘匕首（`Cards=` 是弃牌数）。
  能靠通用规则解释的（如 `Repeat` 变量 → 段数、`CalculatedBlock` → 蜃景）不进例外表。
- **盲区（必须记住）**：抓不到"数字只写在描述里、变量里没有"的错（匕首雨、逃脱计划那类）；
  也**不报覆盖**——没在日志里出现过的牌它一句话都不说，所以"没报不一致 ≠ 验过了"。
- 已接进 `.agents/skills/card-semantics-change` 的验证清单。

**首跑发现（52 处不一致，其中 50 处同一个 bug）**：

| 现象 | 根因 | 影响 |
|---|---|---|
| `防御` 面板=5 / 游戏=7 或 10；`生存者` 8/10；`斗篷与匕首` 6/8 | `Analyze` 里格挡取的是 `ReadByType(BlockVar).IntValue`（**基础值**），伤害却走 `PreviewValue`（含修正） | 本场已有的**敏捷**完全没进模型 → 格挡被低估、"预计掉血"偏悲观、可能多推荐一张防御 |

这与"小刀改走游戏预览值"是同一类问题（手算/取基础值 vs 用游戏预览值），只是发生在格挡上。
（另 2 处 `逃脱计划 draw=0` 是 v0.9.7 修掉的旧 bug，日志是修复前的 —— 反过来说明这套比对能抓到真错。）

**v0.9.9 已修（待实机验证）**：新增 `ReadPreviewByType`（按类型取卡面**预览值**而非基础值），格挡改用它；
本回合新获得的敏捷仍由 `state.Dex` 动态叠加，不会重复计算。
复验需要**新的日志**——旧日志那 52 处是旧构建产生的，再跑仍会报。做法：打一场"本场敏捷 > 0"的战斗
（例如第 1 回合先出灵动步法、第 2 回合看面板上的防御），然后 `python tools/verify-log.py`。

> 中毒/抽牌/小刀等其余字段仍取基础值（`ReadByType`），但在 1613 条牌行里从未与游戏卡面值不符，
> 所以这次只改有实证的格挡，不做无依据的扩大改动。

## v0.9.8（已编译）— 创意工坊发布准备：测试功能编译期分离 + 打包

为发布到 Steam 创意工坊（AppID **2868840**，官方 `ModUploader`）做准备，核心是把"开发方便"与"给玩家干净"分开。

### 编译期分离：一份源码，两种产物

`DamageAdvisor.csproj` 新增 `-p:Workshop=true` 开关（注入编译常量 `WORKSHOP`）：

| | 开发版（默认构建） | 工坊版（`-p:Workshop=true`） |
|---|---|---|
| F9 注入测试牌（`TestCards`/`TickInject`/`InjectNextTestCard`/`InjectAsync`） | 有 | **编译期不存在** |
| 卡池转储（`Entry.DumpSilentPoolOnce`） | 有 | **编译期不存在** |
| 面板提示里的"F9 注入" | 有 | 无 |
| 版本号 | `0.9.8` | `0.9.8-ws`（`Entry.DisplayVersion` 自动拼） |

实测校验（按 ASCII 与 UTF-16LE 两种堆查 DLL 字节）：开发版含 `InjectNextTestCard`/`TestCards`/`DumpSilentPool`、无 `0.9.8-ws`；工坊版全部相反。产物 76 KB → 70 KB。

这样 `DamageAdvisor.json` 的 `affects_gameplay: false` 在工坊版里才**名副其实**——F9 注入会真实修改本地战斗状态（联机时已自动拒绝），发到工坊属于性质问题，不只是措辞问题。

### 打包与发布

- 新增 `workshop/` 工作区（官方 `ModUploader` 格式）：`workshop.json` + `image.png` + `content/`（构建产物，已 gitignore）。
- `build.ps1 -Workshop`：编译 → **校验产物身份**（检出注入/转储代码或缺少 `<版本>-ws` 就报错）→ 填充 `content/`（DLL + json + LICENSE）→ 打印上传命令。开发版那条路径也加了反查：万一增量构建没重编、把工坊版 DLL 部署进 `mods/`，直接报错拦住。
- 新增 **MIT `LICENSE`**，并随包放进 `content/` —— MIT 要求"版权声明随许可声明随副本附带"，而工坊分发的就是 `content/` 里那几份文件。
- 工坊首发用 `unlisted`（发直链给少数人验证），通过后转 `public`；tags 用官方保留给工具类的 `Tools & APIs`；`minBranch`/`maxBranch` 留空。

### 踩到的两个 PowerShell 坑（已写进 deploy-and-release skill）

- `.ps1` 必须存成**带 BOM 的 UTF-8**：Windows PowerShell 5.1 按 ANSI 读无 BOM 文件，中文注释里的字节会把引号吃掉 → 语法错误。
- `Get-Content -Raw` 读 `DamageAdvisor.json` 会因同样的编码问题读坏、`ConvertFrom-Json` 抛异常；改用 `[System.IO.File]::ReadAllText($p, [Text.Encoding]::UTF8)`。

> 验证状态：编译与产物校验已完成（0 warning / 0 error，两产物差异实测确认）；**工坊上传、工坊版实机加载仍未验证**。
> 另：v0.9.7 那 6 张牌（侧步/匕首雨/暴露/刀扇/逃脱计划/终结技）仍待实机核对。

## v0.9.7（已编译，待实机验证）— 按"卡池转储 + 实战日志"逐张复核后的 12 处修复

> 验证状态：`dotnet build` 0 warning / 0 error，产物已部署到 `mods/DamageAdvisor`。
> **尚未实机验证**——侧步/匕首雨/暴露/刀扇/逃脱计划这几张牌在现有日志里没出现过，
> 需要实战（或 F9 注入）跑一遍确认解析行与卡面一致。

这次不是靠读代码猜，而是把 `DamageAdvisor.log` 里游戏自己吐出来的 **91 张猎人卡池转储**
（`kw/desc/vars`）和战斗日志逐条对照，找出建模与实际语义不符的地方。

### 会让推荐"打不出来"或"算错一半"的

| 牌 / 机制 | 现象 | 根因 | 修复 |
|---|---|---|---|
| **侧步 Sidestep** | 本回合能量被凭空 +1，推荐出根本打不出的连招 | 卡面 `vars=[Energy=1]`，但描述是**「下回合，获得 1 能量」**——变量名一样、生效回合不同 | `GainsEnergyNextTurn` 白名单，本回合不计；面板标注"下回合+1能量" |
| **匕首雨 DaggerSpray** | 伤害只有 4 而不是 8 | "造成 4 点伤害**两次**"写死在描述文本里，**没有 `Repeat` 变量**，`HitCount` 取不到就按 1 段 | `HardcodedHits` 表补 2 段（`hits = HitCount × HardcodedHits`） |
| **终结技 Finisher** | 本回合已出过攻击后仍按"第 1 次"算，严重低估 | `Solve` 里 `attacksPlayed` 恒 = 0（旧 TODO），而面板每次手牌变化都会重算 | 基数改为读游戏卡面 **`CalculatedHits` 预览值**（既含已出攻击数又含自身），再叠加计划内的攻击数 |
| **余像 Afterimage** | 出防御这类"自带格挡"的牌时，余像的 +1 格挡被吞 | 三元条件写反：`card.Block > 0 ? 0 : state.BlockPerCard` | 无条件计入，并纳入"融入暗影"的翻倍范围 |
| **暴露 Expose** | 敌人囤的格挡把整轮输出吃光，模型不知道 | 只建模了"给予易伤"，漏了描述里的**「去除敌人身上所有格挡值和人工制品」** | 新增 `RemovesBlock`：结算前清空目标格挡 |
| **刀扇 FanOfKnives** | 多怪战里小刀伤害成倍低估，还要给单体小刀挑目标 | 只读了 `Shivs=4`，漏了**「小刀现在会攻击所有敌人」** | 新增 `ShivsHitAll`：打出后手牌/抽牌堆的小刀统一切成"打全体"模板（Id 加 `_AOE` 后缀以进状态指纹） |
| **逃脱计划 EscapePlan** | 0 费粘手牌被当成纯 3 格挡 | 描述里的"抽 1 张"没有 `Cards` 变量 | `FixedDraw` 表（与 `DaggerThrow` 合并成一张表） |

### 数值/状态类

- **小刀伤害改走游戏预览值**：新生成的小刀以前用手算公式（基础+力量+精准，虚弱按 3/4），
  吃不到"缩小"这类全局修正（实测：被缩小时游戏给打击的预览是 `Damage=6/4.2`）。现在优先取
  场上真实小刀的 `PreviewValue`，手牌和抽牌堆都没有小刀时才退回手算。
- **已虚弱敌人的重复减伤**：`GetSingleDamage` 本身已含敌人"当前虚弱"，旧代码只要 `Weak > 0`
  就再乘 3/4。改为只有"原本不虚弱、本回合被我们打上虚弱"（`WeakThisTurn`）才乘。
- **记忆化指纹补 `AttacksPlayed`**：终结技的伤害取决于它，漏掉会把"攒攻击数再打终结技"的分支剪掉；
  同时补上小刀全体标记与敌人的 `WeakThisTurn`。
- **蜃景 Mirage** 的面板格挡只统计存活敌人（把已死敌人的中毒算进去会偏高）。
- **消影 Fade** 从"给自己加敏捷"改为标记未建模：它 `target=AnyAlly`，描述是**「另一名玩家」**获得敏捷。

### 显示 / 日志 / 稳定性

- 日志里"按名字找怪"改为**按屏幕编号**（同名史莱姆会取错，取不到还会把 `null` 传下去），敌人行补上 `blk=`。
- 面板刷新指纹补上自己的血量/格挡、敌人血量/格挡、抽牌堆张数，避免停在过期建议上。
- `Tick` 整体兜异常（此前只有 `Refresh` 被包住），异常日志按 5 秒限流，避免失效节点每帧写盘；
  `Entry.Attach` 重建节点前先解绑旧的 `ProcessFrame` 回调。
- 清掉死代码：`GrantsDexterity`、`TriggersOnDiscard`、`CountSkillsInHand`、`ShivBlocked`、
  `AnalyzeContext` 的三个未用字段、`_visited.Count < 100000` 这个永远不成立的判断。

### 仍未建模（如实标注，未改）

无实体/幽魂形态（Intangible 会把来袭伤害压到 1，现在会高估）、跟踪 Tracking、谋划专家、
紧勒、猛扑、狩猎的额外奖励、墨之刃的"墨影小刀"（按普通小刀算）、抽牌堆空时不洗弃牌堆。

**伤害取一个采样目标的面板值**：游戏卡面预览是针对"当前主目标"的，搜索里所有目标共用这一个数字。
要按每个目标分别取值得动 `UpdateCardPreview`（会改到卡面显示），暂时保留，改为在面板脚注标明按几号怪算。

## v0.9.6（已编译）— 修正"暗影步"语义 + 弃整手时的奇巧触发

读日志时发现推荐里出现 `防御 → 暗影步 → 生存者` —— **这条根本不可能执行**。

- **根因**：`暗影步 (ShadowStep)` 的 `Cards=3` 被我当成"抽 3 张牌"，但它的真实描述是
  **「丢弃所有手牌。在下个回合，你所有的攻击伤害翻倍。」** —— `Cards` 变量在这里**不是抽牌数**。
- **修复**：
  - 从 `CardsMeaning.Draw` 里移除 `ShadowStep`，新增 `DiscardsEntireHand` 标记；
  - 出牌时清空手牌、不补充，并计入 `DiscardedThisTurn`（影响铭记死亡）；
  - 面板标注"弃整手（下回合攻击翻倍，未建模）"——**下回合的翻倍效果不建模**，如实说明。
- **顺带修复**：弃整手时（钢铁风暴/计算下注/暗影步），手里的**奇巧(Sly)牌现在会正确触发自动打出**
  （求解器的 `DiscardAndDraw` 就是这么做的），此前是直接清空、Sly 效果丢失。

### 复核结论

把 12 张"抽牌/生成小刀"类牌逐张对照描述核对过：只有暗影步判错，其余（杂技/肾上腺素/后空翻/独门技术/早有准备/本能反应/墨之刃/刀刃之舞/斗篷与匕首/袖里乾坤/隐秘匕首）全部正确。

## v0.9.5（已编译）— 修掉两处自查发现的回归/顺序 bug

读实战日志时发现两张牌被错误标记：

| 牌 | 现象 | 根因 | 修复 |
|---|---|---|---|
| **精确切击** | `dmg=0 ok=False 未建模`（实际 `CalculatedDamage=16/6`，即 16 − 2×手牌5 = 6） | **我修可空警告时把 `CalculatedDamage` 兜底删掉了**，只剩 `Damage` 一个键，计算型伤害牌全部读不到 | `EstimateDamage` 恢复两键兜底（先 `Damage` 再 `CalculatedDamage`），同时保持 0 可空警告 |
| **钢铁风暴** | `ok=False note=弃整手，每张换小刀`（实现其实早已写好） | "未建模"判定在机制赋值**之前**执行：先被判 False，随后 note 被覆盖，`supported` 却已经定死 | 判定移到所有机制计算之后，并把特殊机制（弃整手/手牌免费/奇巧加成/能力联动/双倍格挡…）计入"有效果"判断 |

顺带清掉了一个被删除代码块留下的孤立右括号（导致编译失败）。

教训（已同步进 skills）：**判定顺序 —— 先算完所有效果，再判"是否未建模"**；删代码块后要检查花括号平衡。

## v0.9.4（已编译，待部署）— 平局时不再浪费能量

日志发现：`精准`（能力牌）在本回合没有小刀时仍被打出（纯浪费 1 能量），例如
`计划 防御 -> 精准 -> 打击[1号]`。原因是评分里没有"平局时的资源判定"，DFS 先找到哪条就用哪条。

修复：两种模式的最后都加一条 **完全平局时少花能量优先**（`EnergySpent` 更小者胜），
避免为了"本回合没有收益的能力牌"白扔能量。同时把 `耗能/回能` 写进日志的"计划"行，便于核对。

### 本轮日志核对结论（v0.9.3 实机）

- 无异常；`nodes` 从撞 80000 上限降到 **1~84**（记忆化生效）。
- 手工核对两条推荐均正确，例如：
  `HP46 来袭9(3+6) → 打击[1号](6伤恰好斩杀5血) + 生存者(8格挡) → 掉血0`。
- 精准/小刀/毒/易伤/奇巧弃牌等链路均在推荐里正常出现。

## v0.9.3（已部署）— 修正"剩余能量"显示为负

使用者反馈面板出现 `剩 -1 能量`。

- **搜索侧能量记账是正确的**（有 `effectiveCost > state.Energy` 的可行性检查，且 `Energy = Energy - cost + gain`）。
- **问题在显示**：面板用 `初始能量 - 总花费`，没把"回能"加回来。而奇巧（Sly）牌被弃会自动打出，`战术大师` 这类会 +1 能量，于是花费可以合法超过初始能量。
- 修复：`剩余 = 初始能量 + 回能 - 花费`；有回能时显示 `（耗 4·回 1）` 便于核对。
- 顺带修复：**弃牌触发的回能没有计入 `EnergyGained`**（只加了实际能量，没进统计），这也是显示为负的原因之一。

## v0.9.2（已部署）— 随状态变化的牌改为出牌时实时计算

用户指出：终结技这类牌应该"尽量往后排"。核查发现三张牌的收益被**提前算死**了：

| 牌 | 问题 | 修复 |
|---|---|---|
| 终结技 Finisher | 按"本回合已打出攻击牌数"×6，但计数恒为 0（Analyze 时算死）→ 永远只算 6 伤 | 增加 `SearchState.AttacksPlayed`，出牌时算 `Damage × (已打出攻击数 + 1)`（含自身）→ 搜索自然把它排到最后 |
| 飞镖 Flechettes | 用了"整手牌数"而不是"手牌中技能牌数"，且提前算死 | 出牌时按 `next.Hand` 里的技能牌数实时计算 → 会被排到最前 |
| 蜃景 Mirage | 格挡用了"进入搜索时"的敌人中毒，没算搜索中刚上的毒 | 出牌时按当前敌人中毒总和计算 |

实现上引入 `ScalingKind`（None / AttacksPlayed / SkillsInHand / EnemyPoisonTotal），
在 `PlayCard` 里统一实时计算，`AttacksPlayed` 也纳入搜索状态与状态指纹。

## v0.9.1（已部署）— 搜索记忆化，消除排列爆炸

日志暴露出：手牌 10 张、含 0 费小刀与弃牌分支时 `nodes` 顶到 80000 上限（**搜索被截断**）。

原因：DFS 会把"出牌顺序不同但结果等价"的路径重复展开（A→B 与 B→A 走两遍）。

修复：给搜索加**状态指纹记忆化**（能量/格挡/敏捷/弃牌数/各类标记 + 手牌集合 + 抽牌堆 + 敌人状态），
重复状态直接返回。`Nodes` 现在计的是**唯一状态数**，因此上限从 80000 降到 25000
（覆盖范围更大、又不会在主线程造成卡帧）；访问集合上限 100000 条。

### 同批次验证到的正确行为（来自实机日志）

- 奇巧（Sly）链路生效：`隐秘匕首` 弃掉 `战术大师`（+1 能量）+ `本能反应`（抽 2 张）→ 两者自动打出，凑出 4 点能量的动作序列。
- 毒杀 + 精确格挡：`刺杀`（易伤）→ `带毒刺击` 被易伤放大到 9 伤，21 血怪剩 2 血、8 层毒先手击杀 → 它的 9 点来袭消失；3 张小刀打另一只，`斗篷与匕首 + 灵动步法` 给出正好 8 格挡 → **HP 6 时做到 0 掉血、31 伤害**。

## v0.9.0（已部署）— 卡牌词条（keywords）支持

对猎人卡池 91 张牌做了词条盘点：`Exhaust ×11`、`Sly ×7`、`Innate ×3`、`Retain ×2`。

| 词条 | 处理 |
|---|---|
| **Sly（奇巧）** | **之前只硬编码了战术大师/本能反应，其余奇巧牌被弃时效果全部丢失**。现在按游戏语义实现：`Sly` 牌被丢弃会**自动打出**（`AutoPlayType.SlyDiscard`，不花能量），伤害/格挡/抽牌/能量/中毒/敏捷/小刀都按自动打出结算；弃牌选择也改为**优先弃奇巧牌**并挑价值最高的那张 |
| Unplayable | 用 `card.Keywords.Contains(CardKeyword.Unplayable)` 判定，标记为不可打出、不参与推荐（`IsPlayable` 是 protected，用不了） |
| Exhaust / Retain / Innate / Ethereal | 本回合不影响结果（消耗不影响"本回合打一次"，保留/固有/虚无影响后续回合），已在 README 说明 |
| 词条显示 | 面板手牌行显示 `[奇巧/消耗/保留/固有/虚无]`；判定源为 `card.IsSlyThisTurn`（含"本回合获得奇巧"的情况） |

### 奇巧牌的完整清单（会自动打出）

`战术大师`（+能量）、`本能反应`（抽牌）、`翻越撑击`（7 伤全体）、`连续反弹`（3 伤×4）、`触不可及`（6 格挡）、`磨蚀`（敏捷+荆棘）、`鬼祟`（能力）

## v0.8.1（已部署）— 读取敌人身上的 debuff

**结论**：敌人身上的中毒/易伤/虚弱/力量可以直接从 `Creature.Powers` 读（`GetPower<T>()?.Amount`），队友施加的也算数——**不需要预测队友行为**。

已改动：

- **模拟的初始中毒 = 敌人当前中毒层数**（之前从 0 起算，导致队友/上回合留下的毒没进"会不会被毒死"的判断）。
- 面板敌人行显示 debuff：`1.化石追踪者 40/40(来袭12) [中毒5 易伤2]`。
- 日志敌人行追加 `buff[中毒=5 易伤=2 虚弱=1 力量=-2]` 与 `按我算/按全队算`。
- 注意：**虚弱/力量不初始化进模拟**——因为 `GetSingleDamage` 已经把它们算进 `BaseIncoming` 了，再初始化会重复计算；模拟里的 `Weak/StrengthLoss` 只代表"本回合新施加的部分"。

### 仍需注意

- 卡面伤害取的是游戏预览值（含易伤），但预览对应"当前/上次悬停的目标"，多怪且易伤不对称时会有偏差。

## v0.8.0（已部署）+ 联机结论修正

- 新增 **F6 优先级模式**：保命优先（默认）/ 输出优先。
- 来袭伤害改为按"自己"作为目标计算。
- **联机结论（由使用者确认）**：《杀戮尖塔 2》联机下敌人攻击对**全队造成相同伤害**，因此"按我算"即为准确值——原先"目标归属不确定、结果偏保守"的判断是错误的，已修正文档与注释。
- 联机下真正仍未覆盖的只有：队友的输出/中毒/易伤，以及"队友先把怪打死"带来的乐观偏差。

## v0.7.2（覆盖率审计后的修复，已部署）

用 `silent-coverage-audit` 的思路把 91 张猎人牌与代码对照，发现并修掉 4 个**真实 bug**：

1. **全体中毒牌完全没生效（严重）**：`迷雾（Haze，全体 4 毒 + 1 虚弱）` 与 `毒性爆发（Outbreak，全体 9 毒）` 这类 `AllEnemies` 牌在搜索里走的是"无目标"分支（targetIndex=0），而 debuff 代码只按编号取单体目标 → **毒与虚弱一个都没加上**。现在改为：`HitsAll` 的牌对全部存活敌人施加 debuff。
2. **毒性爆发的"立即触发中毒"未实现**：现在给完毒后立刻结算一次中毒伤害（中毒伤害无视格挡，另开了 `ApplyDamageIgnoringBlock`）。
3. **弹跳药瓶（BouncingFlask）只算了 1 次**：描述是"随机给予 3 层中毒 ×3 次"，总量应为 9；现在按 `Repeat` 乘算（仅对无毒伤但有 `Repeat` 的牌）。
4. **铭记死亡（MementoMori）漏算弃牌加成**：`本回合每弃一张牌 +4 伤害` 现在按 `SearchState.DiscardedThisTurn` 加算。

同时这次审计也确认了一批"看起来特殊、实际已正确处理"的牌：匕首雨/回响斩击/翻越撑击（全体攻击）、尖啸（全体力量削减）、精密瞄准/狩猎（单体高伤）、连续反弹（多段随机，已标注随机目标）。

### 审计方法（可复用）

用 `docs/silent-pool.txt`（卡池转储）筛出"描述里含特殊机制但代码未点名"的牌，再逐张对照描述与 `vars`：

```powershell
# 筛出可疑牌（含 特殊关键字 且 SilentLogic 未点名）
$rows | Where-Object { # DamageAdvisor 进度与状态

## v0.7.1（已修，待部署）

静态自查发现的 4 个问题，已修复：

1. **敌人格挡未计入**（P1）：`SimEnemy` 增加 `Block`，`ApplyDamage` 先扣格挡再扣血——之前遇到会加格挡的怪会高估伤害、误报"可斩杀"。
2. **弃牌触发的抽牌绕过抽牌副作用**（P1）：`本能反应` 被弃时的抽牌改走 `HandleDraws`，腐蚀波/速行者的抽牌副作用不再漏算，且尊循子弹时间的"本回合不能抽牌"。
3. **搜索去重键过粗**（P2）：去重条件补上 `Block/Poison/Shivs/Vulnerable/Weak`，避免把"同名但状态不同"的牌误判为重复。
4. **cfg 运行中不重载**（P3）：面板每 5 秒检查一次 `DamageAdvisor.cfg` 的修改时间，外部改配置即时生效（日志会打印"配置已重新加载"）。

同时修掉 3 个可空性警告（`TryGetValue` 的 out 参数），现在构建是 0 error / 0 warning。

### 仍未解决（已知）

- 多怪时伤害取数依赖游戏维护的单一 `PreviewValue`（对应当前/上次悬停目标），敌人易伤状态不对称时会有偏差。
- 面板拖动的坐标换算未实机验证。
- 联机不按队友分摊来袭；能力牌的跨回合价值不建模。

## v0.7（已编译，待部署）

### A. 允许掉血上限（面板交互）
- **F7** 循环切换 `0 → 1 → 2 → 3 → 5 → 0`（0 = 原来的严格保命）
- 存到 `mods\DamageAdvisor\DamageAdvisor.cfg`（`hpLossBudget=` / `collapsed=`），启动时读取
- 面板标题实时显示"允许掉血 ≤N"

### B. 跨回合视野（优先击杀）
- 评分顺序改为：**不死 → 掉血 ≤ 预算（超预算则少掉血优先）→ 优先击杀 → 最大伤害**
- 击杀的跨回合价值：打死一只怪 = 省掉它下一轮的攻击（同时本回合它的来袭也归零）

### C. 面板交互
- **鼠标拖动**面板任意位置可移动（轮询实现，不依赖 Godot 输入回调）
- **F10** 折叠/展开（折叠后只留"推荐"和"斩杀"两行）
- 按键总览：`F7 掉血上限` / `F8 隐藏` / `F9 注入测试牌` / `F10 折叠` / 拖动移动

## v0.6（已编译，待部署）

- X 费牌：串刺（8 伤 × 能量）、萎靡（X 力量削减 + X 虚弱）
- 子弹时间：手牌全部免费 + 本回合禁止抽牌
- 钢铁风暴：弃整手换等量小刀
- 计算下注：弃整手抽等量
- 精准：本回合小刀 +4
- 余像：每打一张牌 +1 格挡
- 暴露：施加 2 层易伤
- 修复：精确切击等计算型伤害读 CalculatedDamage；华丽收场真实检查抽牌堆；蜃景不再误标未建模
- F9 注入：手牌满 10 张时先弃最后一张


最后更新：2026-09-15 15:12

## 当前版本：v0.5（猎人机制版，已部署）

### 功能

| 功能 | 状态 |
|---|---|
| 手牌伤害预估（复用游戏 `DamageVar.UpdateCardPreview`） | ✅ |
| **保命优先**：不死 → 少掉血 → 最大伤害（字典序） | ✅ 已验证 |
| 怪物编号 1/2/3 + 计划里标明打哪只 | ✅ 已验证 |
| 斩杀提示（✅击杀 / ☠中毒先手击杀 / 残血 x→y） | ✅ 已验证 |
| 打死怪不再计入来袭 | ✅ |
| 虚弱 -25% 来袭、中毒先手击杀（敌方行动前） | ✅ 已验证（中和后意图 11→8） |
| **猎人卡池 91 张逐张语义**（`src/SilentLogic.cs`） | ✅ 已接 |
| 小刀生成 / 抽牌分流（`Cards` 变量重载修正） | ✅ 已接 |
| 小刀伤害 = 基础 + 力量 + 精准 | ✅ 已接 |
| 弃牌 + 弃牌触发（战术大师给能量、本能反应抽牌） | ✅ 已接 |
| 能量 / 敏捷 / 易伤 / 力量削减 | ✅ 已接 |
| 计算型伤害（终结技、飞镖、蜃景） | ✅ 已接 |
| 未建模的牌显式标注原因 | ✅ 已接 |

### 数据来源（重要）

- 猎人卡池转储：`docs/silent-pool.txt`（91 张，含**卡牌描述文本** + 机制变量）
- 卡牌语义交叉验证：CombatSolver 的 mirror 实现（`CardPileOnPlaySupport` / `CardChoiceSupport` 等）
- 关键结论：`Cards` 变量**被重载**——`刀刃之舞/Cards=3` 是生成 3 张小刀，`杂技/Cards=3` 是抽 3 张牌；
  `隐秘匕首/Cards=2` 是弃 2 张、`Shivs=2` 是生成 2 张小刀。

### 已知局限

- 小刀伤害用"基础+力量+精准"估算，**未计入易伤之外的其它修正**。
- 弃牌是确定性选择（优先弃可触发牌，其次弃期望值最低的），没有对"弃哪张"做完整搜索。
- 能力牌按"本回合无即时收益"处理（但准确/毒雾这类会跨回合生效）。
- X 费牌、需空抽牌堆的牌、以及 `SilentLogic.UnsupportedReason` 里列出的牌不参与推荐。
- 联机：只算自己，敌人意图按"对全队"的总伤害计算，因此预计掉血偏保守。

## 关键实现点（避免重复踩坑）

- UI 挂到 `NGame.Instance`；挂 `SceneTree.Root` 的延迟调用会落空。
- 动态加载的 Mod 程序集里 **Godot `_Ready`/`_Process` 虚函数不会被派发** → 显式 `EnsureStarted()` + `SceneTree.ProcessFrame` 信号驱动。
- 卡面效果**按变量类型匹配**（`BlockVar` / `PowerVar<PoisonPower>` / `CardsVar` / `RepeatVar`），不猜键名。
- `ModelDb` 在 Mod 初始化时还没建好 → 卡池转储放在**第一场战斗**时做。
- 直接跑 exe 会因缺 appID 导致 Steam 初始化失败 → **必须经 Steam 启动**。

## 构建 / 部署 / 测试循环

```powershell
Stop-Process -Name SlayTheSpire2 -Force            # 1. 关游戏（DLL 被占用）
pwsh -File "…\moddev\DamageAdvisor\build.ps1"       # 2. 构建 + 部署
Start-Process "steam://rungameid/2868840"           # 3. 经 Steam 启动
Get-Content "…\mods\DamageAdvisor\DamageAdvisor.log" # 4. 看日志
```

日志格式：`手牌快照#N … incoming=…` → 每张牌（含原始 `vars=[…]`）→ 每只怪意图 → 最终计划。
