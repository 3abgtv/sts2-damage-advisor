# 构建产物
bin/
obj/
# 本地运行产物 / 调试
shots/
*.log
*.log.old
*.cfg
# 游戏卡池转储（含游戏内文本，不随仓库分发）
docs/silent-pool.txt
# 本地 agent 配置（.claude/skills 是指向 .agents/skills 的目录联接，各机器自建）
.claude/
# 工坊上传内容由 build.ps1 -Workshop 填充（构建产物），工作区其余文件照常跟踪
workshop/content/
# 日志验证报告（tools/verify-log.py 输出，含游戏内文本）
docs/verify-report.txt
